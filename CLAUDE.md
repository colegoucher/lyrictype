# LyricType — Project Instructions

## Auto-commit rule
After completing any task (bug fix, feature, refactor, UI change), **always commit without being asked**. Stage only relevant source files (never `.env` or secrets). Write a concise commit message describing what changed and why.
